Given fsharp:
Do:
  i
  let func =\<CR>
  doSomething\<CR>
  |> doOtherThing\<CR>
  |> andSomethingElse

Expect:
  let func =
    doSomething
    |> doOtherThing
    |> andSomethingElse

Do (Multiline pipeline within match):
  i
  match thing with\<CR>
  | case 1 ->\<CR>
  resultExpression\<CR>
  | case 2 ->\<CR>
  multilineExpression\<CR>
  |> doSomethingWithResult\<CR>
  |> doAnotherThing

Expect:
  match thing with
  | case 1 ->
    resultExpression
  | case 2 ->
    multilineExpression
    |> doSomethingWithResult
    |> doAnotherThing

